# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
# 例外：app/build/ 是 electron-builder buildResources（应用图标 icon.icns / icon.png），需入库
!app/build/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env
.env.*
!.env.example
workspace/

# Runtime data (per docs/DESIGN.md §3.7) —— workspaces / transcripts / cursors。
# room.toml 是 shipped config，跟代码走；房间运行时数据全部忽略。
# 裸 `guests/` 同时盖住顶层 isolation=global 茶客 workspace 和 rooms/*/guests/ 嵌套目录。
guests/
rooms/**/transcript.jsonl
rooms/**/summary.jsonl
rooms/**/cursor.json
# P6 调试落盘：prompt 文件含 USER.md / 工具参数 / 任务上下文（与 .env 同口径），不入仓 / 不随 export。
rooms/**/debug/

# Local agentao residue (from previous experiments in this dir, if any)
.agentao/
agentao.log
agentao.log.*

# Claude Code 本机运行时状态（scheduled_tasks.lock 含 sessionId / pid / 进程起始时间），
# per-machine 不入仓——否则别的 checkout 会继承陈旧锁 + 本地会话元数据。
.claude/

# 头像源图（高清原图）。压缩版（128px ~30KB）在 chahua/personas/<name>.png，
# 跟 persona .md 一起 commit；原图保留在本地作为可重新压缩的来源。
docs/personas/*.png

# Macs being Macs
.DS_Store

# Qoder 
.qoder/
