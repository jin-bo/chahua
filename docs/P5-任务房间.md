# P5：从「群聊」升级到「任务房间」

> 目标：在不破坏「聊天是主界面」的前提下，把房间从纯聊天容器升级为**带任务的工作容器**：一个房间可以有多个任务，每个任务有目标 / 状态 / 负责人 / 产物文件 / 决策记录；右侧（或下方）面板能看到"正在产出什么"。

**状态：设计中（2026-05-16）**。本文为方案草案；落地拆分见 §7。

---

## 1. 现状速写 & 关键观察

| 现有 | 与"任务房间"的关系 |
|---|---|
| `Room` + `transcript.jsonl`（flat 流） | 任务**附加**在它之上，不取代 |
| `summary.jsonl`（房间级增量摘要） | 与"任务级摘要"共存，分目录 |
| `share/`（用户上传共享目录）+ `upload.js` | artifact 的搬运基础设施已就位 |
| `[[guest]].cwd` 受 `isolation = room \| global` 决定 | 茶客自动产出归到 task 目录 = 改 cwd 语义，**不在 P5.1 做** |
| `ChahuaEnvelope.schema_version=1` | 新加 `data.task_id` 字段无须 bump；新事件 type 走"未知 type 前端 WARN 跳过"的兼容口径 |

**最重要的判断**：聊天仍是线性主流，task **不分裂消息树**，只是给消息打 tag。Slack thread 那种"消息分裂到子流"会破坏茶客之间"听见对方"的语义（打分上下文取最近 K 条 → 分裂后 K 条不再连续），代价远大于收益。

---

## 2. 核心模型（四个新概念）

```
Room ─┬─ Task[]               # 一个房间多个任务，串行的"活跃任务"语义
      ├─ transcript.jsonl     # 仍是一条线，每条多一个 optional task_id 字段
      └─ summary.jsonl        # 房间级摘要不动

Task ─┬─ task.json            # 元数据：title/goal/status/owner/created_at
      ├─ decisions.jsonl      # append-only：被"采纳"的决策（指向 message_ids）
      ├─ artifacts/           # 任务产物文件目录
      ├─ events.jsonl         # P5.2 起：状态/负责人/title 变更历史
      └─ summary.jsonl        # P5.2 起：任务级增量摘要
```

### 2.1 Task 数据契约

```python
@dataclass(frozen=True)
class Task:
    id: str                  # task_<10字节 hex>，同 message_id / turn_id 一根
    title: str               # ≤ 60 字
    goal: str                # markdown，多行
    status: Literal["open", "in_progress", "blocked", "done", "abandoned"]
    owner: Optional[str]     # speaker_id（user 或茶客名）；None = 全员
    created_at_ms: int
    updated_at_ms: int
    closed_at_ms: Optional[int]
```

### 2.2 Thread 不是新实体

"同属一个 task_id 的消息"= 一条 thread，UI 上是个 filter 视图，不另存。

### 2.3 Decision

```jsonc
{ "decision_id": "dec_...", "task_id": "task_...",
  "supporting_message_ids": ["msg_...", "msg_..."],
  "summary": "<≤200 字一句话>",
  "marked_by": "user",  // P5.1 仅用户能采纳
  "ts_ms": ... }
```

### 2.4 Artifact

不是新数据库实体，**就是 `tasks/<task_id>/artifacts/` 目录下的文件**。索引在文件系统本身。

---

## 3. 落盘布局

```
rooms/<room-id>/
├── room.toml               # 不变
├── transcript.jsonl        # +task_id 字段（可缺 = 房间级闲聊，向后兼容）
├── summary.jsonl           # 房间级，不变
├── cursor.json             # 不变
├── guests/                 # 不变
└── tasks/                  # 新
    ├── state.json          # {active_task_id?}，覆写式 tmp+rename；空房间允许缺
    └── <task_id>/
        ├── task.json       # 覆写式 tmp+rename，全量元数据
        ├── decisions.jsonl
        ├── artifacts/      # 文件
        ├── events.jsonl    # P5.2 起；P5.1 没有 status / owner / title 变更
        └── summary.jsonl   # P5.2 起；P5.1 不写任务级摘要
```

**为什么没有 `_index.jsonl`**：单房间任务数大概率个位数到十几个，启动时直接扫 `tasks/*/task.json` 拼列表足够；省一个"真源 + 索引一致性"的小坑。等真撞到性能问题再加。

**为什么单独留 `state.json`**：`active_task_id` 是消息归属的核心状态，必须跨重启 / reconnect / 切房存活，否则用户消息会悄悄落回"房间级闲聊"。它与任务列表无关，独立一个覆写文件比塞进 cursor.json / 散到 task.json 都干净（一个任务被另一个 user action 设为非 active 不该改它自己的 task.json）。

**为什么 task.json 用覆写而非 append**：title / status 是频繁更新的元数据，append-only 历史价值低；状态历史 P5.2 起交给 events.jsonl 存。这与现有 cursor.json 同口径。

---

## 4. 事件协议增量

### 4.1 Envelope `data.task_id`（顶层不动）

`message_start` / `message_end` 的 `data` 多一个可选 `task_id`。前端按这个分流到任务面板。**schema_version 不 bump** —— 老前端忽略未知 data 字段不出错。

### 4.2 新增 event types

**P5.1**：

| type | 时机 | data |
|---|---|---|
| `task_info` | ws 连上 / 切房 / **active 变化**时重发 | `{tasks: [...], active_task_id?}` |
| `task_open` | 用户开任务 | full task.json |
| `task_update` | title / goal 变更（**P5.1 不含 owner / status**；owner 在 P5.1 锁定为创建时值） | diff |
| `task_decision_added` | 决策入库 | decision 全量 |
| `task_artifact_added` | artifact 入目录（上传 / 手动绑） | `{task_id, name, size, rel}` |

P5.1 不为 "active 切换" 单独定义事件 —— `task_open` 后 server 立刻重发整份 `task_info`，前端按 `active_task_id` 字段更新指针。少一个事件少一个 race 面。

**事件分工：`task_info` 是权威快照，其它是 hint**。`task_info` 永远代表 server 端当前真状态；`task_open` / `task_update` / `task_decision_added` / `task_artifact_added` 只用于局部反馈（toast / 动画 / 高亮增量项），不进任务状态本身。前端任务状态以最近一次 `task_info` 为准。这条契约让 P5.1 实现者不必纠结"task_open 与 task_info 同帧发是不是双重广播"——是，且故意。

**P5.2 追加**：

| type | 时机 | data |
|---|---|---|
| `task_close` | done / abandoned | `{task_id, status, closed_at_ms}` |
| `task_update`（扩展） | owner / status 进入 patch 范围 | 同上但 patch 可含 owner / status |

### 4.3 Inbound（前端 → 服务端）

**P5.1**：

```
open_task         {title, goal, owner?}                      # owner 仅在创建时可设
update_task       {task_id, patch: {title?/goal?}}           # P5.1 仅改 title / goal
attach_artifact   {task_id, share_rel}                       # copy 不 move
add_decision      {task_id, supporting_message_ids, summary}
```

P5.1 故意**不提供** `set_active_task` 与 `close_task`，且 `open_task` 在房间已有任意任务时直接拒绝（详见 §7.1 "一房间最多 1 个任务" 实现项）—— 要么没任务、要么有那一个任务，不留切换 / 隐身 / 清空中间态。这是为了把"切 active 时 turn 在跑怎么办"/"已关闭任务怎么渲染"/"用户找不回旧任务"三个面完整压到 P5.2。

**P5.2 追加**：

```
set_active_task   {task_id | null}
close_task        {task_id, status: "done"|"abandoned"}
# update_task 的 patch 在 P5.2 扩展为也能改 owner / status
```

**`attach_artifact` 是 copy 不是 move**：share/ 是房间公共桌面，茶客 cwd 路径、历史消息里引用的 `share_rel`、同一文件可能挂多个任务 —— 移动都会断引用。MVP 一律走拷贝（茶客视角 share/ 永远在原位）；后续如有"清理 share/" 需求再单独提供 GC 入口，不与 attach 耦合。

### 4.4 用户 / 茶客消息的归属

- 用户消息：**P5.1 inbound 不带 task_id**；server 端在 append transcript 时按当前 `state.json.active_task_id` 自动打 tag。前端没切换 UI 也就没有"想发到任务 X 但 server 是任务 Y"的意图分歧，省一个校对面。P5.2 加切换 UI 后再评估是否让前端 inbound 带意图字段。
- 茶客消息：`orchestrator` 在 `turn_start` 时记 `active_task_id`，本轮 `message_*` envelope 都打这个 tag
- `active_task_id` 落 `rooms/<id>/tasks/state.json`，**server 端是真源**；变化时 server 重发整份 `task_info` envelope（不发增量），前端只镜像不投票
- **P5.1 单房间最多 1 个任务**（见 §7.1 限制条款）；P5.2 起才允许多任务共存，仍维持"同一时刻 0 或 1 个 active"

---

## 5. UI 增量

### 5.1 P5.1 UI（窄路径，无切换 / 无关闭）

```
┌─────────────┬──────────────────────────┬──────────────────┐
│ sidebar     │  chat 主区                │ 任务面板 (可折叠) │
│ (不动)      │                          │                  │
│             │  ...聊天滚动...            │ [+ 新任务]        │
│             │                          │ ──               │
│             │                          │ 📌 当前: 写 README│
│             │                          │ 目标: ... ✏      │
│             │                          │ 负责人: @汪小姐   │
│             │                          │ ──               │
│             │                          │ 产物 (3)         │
│             │                          │ • README.md      │
│             │                          │ • plan.md        │
│             │                          │ • demo.png       │
│             │                          │ ──               │
│             │                          │ 决策 (2)         │
│             │                          │ • 用 Electron 打包│
│             │                          │ • 砍掉 Win 支持   │
└─────────────┴──────────────────────────┴──────────────────┘
composer: [+] [📋 写 README] [输入文字...]  [发送]
```

- **composer 上的任务 chip**：纯展示，**没有下拉 / 没有"清回房间级"按钮**。chip 在无 active 时整体隐藏。
- **任务面板"+ 新任务"**：弹小表单（title + goal + owner?），提交后该任务立即成为 active。**`task_info.tasks.length > 0` 时按钮 disabled**（看任务存在性，不看 active —— 防 state.json 丢失情况下悄悄允许开第二个），tooltip 显示"P5.2 起支持多任务"。这是 §7.1 的"一房间最多一任务"约束的 UI 落点。
- **任务卡上 ✏**：仅 title / goal 可 inline 编辑（走 `update_task`）。**owner 在 P5.1 锁定为创建时值**（owner 在 "+ 新任务" 表单里选定后不再可改）；改 owner / status 下拉 / 关闭按钮等放 P5.2。任务卡上 owner 行展示为静态文本，不带 ✏。
- **消息气泡右键 → "📌 标为决策"**：弹小框让用户写一句 summary，引用当前消息和往上选连续几条
- **任务面板可整体折叠**：默认展开；记忆到 user prefs；窄屏自动收

### 5.2 P5.2 UI 追加

- 任务面板底部 "历史任务 ▶" 折叠列表
- composer chip 变成下拉，可切 active / 清回房间级
- 任务卡上加 status 下拉 + "完成" / "放弃" 按钮（走 `close_task`）
- 非 active 任务的旧消息气泡左上角加灰 chip 显示任务名，点击进入该 thread 的 filter 视图

---

## 6. 怎么把 task 信息 / artifact 共享给茶客

三通道，三阶段铺开。各通道在各阶段的状态：

- **P5.1**：三通道全空 —— 茶客对 task 零感知，只通过聊天上下文间接知道用户在做什么；artifacts 不在茶客 cwd 范围
- **P5.2**：通道 2 首次出现 **只读** `./task/` 链接（茶客能"看到"当前任务产物文件），但 prompt / tool 仍不感知 task 概念
- **P5.3**：通道 1 / 通道 3 同时启用 —— 茶客真正"长出 task 视野"
- **P5.4**：通道 2 升级到读写（自动归集茶客产出）；或者直接把 cwd 切到 task artifacts/

### 6.1 通道 1：prompt 注入（task info → 茶客 LLM 视野）— P5.3 启用

只在 `active_task_id` 非空时启用。在 onboarding 末段（rules 之后、最近原文之前）插一块：

```
当前任务：<title>
目标：<goal>
状态：<status>，负责人：<display_name | "全员">
近期决策（最近 5 条）：
- <decision_summary_1>
- <decision_summary_2>
当前产物清单（不嵌内容）：
- README.md (2.3 KB, 2026-05-16 14:23)
- plan.md   (1.1 KB, 2026-05-16 13:50)
- demo.png  (87 KB,  2026-05-16 13:21)
```

**不**把 artifact 文件内容嵌进 prompt —— 大文件爆上下文；让茶客按需走通道 2 / 通道 3 拿。

**已 closed task 的消息**：onboarding 阶段不喂原文，只把 task 的最终 summary 当一句历史摘要喂进"近期梗概"块。

**scoring 加权**（仍是 P5.3 配套）：owner == 该茶客名时打分 ×1.15（cap 到 1.0），鼓励"负责人多接话"。owner = None 不加权。

### 6.2 通道 2：文件系统（茶客读 artifact）

| 阶段 | 茶客视角 | 实现 |
|---|---|---|
| **P5.1** | **没有直接路径** | `attach_artifact` 是 copy，share/ 副本仍在原位；茶客继续通过 `./share/<file>` 读原始文件。artifacts/ 在 P5.1 仅给 UI / 持久化用，不进茶客 cwd |
| **P5.2** | `./task/<file>` **只读** | 每个 guest workspace 下放 `./task/` 软链指向 `rooms/<id>/tasks/<active_id>/artifacts/`；active 切换时重链。**只读** —— 茶客能 `ls` / `cat` 但不能 write |
| **P5.4 远期** | 读写 + 自动归集 | 二选一：① 升级 `./task/` 为 read-write 且 server 端监听 / tool wrapper 拦截 write，合成 `task_artifact_added`；② 直接把 guest cwd 切到 `tasks/<id>/artifacts/`，废软链。两条路都要重设 isolation 语义，与 `[[guest]].isolation` 现有口径合并评估 |

**为什么 P5.2 选只读、把"自动归集 write"延后**：read-write 引入两套实现方向（文件系统 watch vs tool wrapper 拦截），都要面对"agentao 写工具的 cwd 边界 + symlink 是否被穿透 + 写到一半 active 被切"三类边界情况。P5.2 只做"茶客能看到产物"已经覆盖"@汪小姐你看下 README 里第 3 节"这种核心用例；自动归集留 P5.4 一次做对。

**P5.2 软链的平台兜底**：POSIX 用 symlink；Windows 用 junction（目录链，无需管理员权限）。`./task/` 名字与 `./share/` 对仗 —— 见名知是任务级桌面。

**只读怎么落地**：`./task/` 解析到 task artifacts/ 路径在 guest workspace cwd 子树**之外**，沿用 agentao 既有 `workspace-write` 权限模式 —— 写工具按 cwd 边界拒绝外部路径即天然只读，不需要额外路径白名单。`full-access` 模式的茶客仍能写但不合成 `task_artifact_added` —— 这种茶客本就有越界写权限，"产物归任务"的语义在 P5.2 不保证。

**active 切换时**：先解 `./task/` 软链再重建。P5.2 切 active 必须先 cancel 当前 turn（沿用 P3.3 cancel 状态机），cancel 完才动软链 —— 避免茶客 read 读到一半路径被切。

### 6.3 通道 3：工具调用（茶客主动查询 / 提议）— P5.3 起

新增几个 task-aware tool 给 agentao 注册：

| 工具 | 行为 |
|---|---|
| `task_list_artifacts()` | 拿当前 active task 的产物清单（重发 §6.1 的格式）—— 茶客中途想查时不必等下次 onboarding 注入 |
| `task_propose_decision(summary, supporting_message_ids?)` | 提议一条决策，UI 渲染成"采纳"按钮等用户点 |
| `task_propose_open(title, goal)` | 提议开新任务，同样 UI 采纳后才入库 |

**走"提议"不走"直接写"**，沿用 §8 invariant #3：写权限永远在用户，茶客不能静默开任务 / 记决策。"采纳"按钮是 UI 把茶客的 propose 转译回 inbound `add_decision` / `open_task` 的薄包装。

### 6.4 三通道 vs 阶段对照

| 通道 | P5.1 | P5.2 | P5.3 | P5.4 |
|---|---|---|---|---|
| prompt 注入 | — | — | ✓ 启用 task 块 + scoring 加权 | — |
| 文件系统 | 仅 share/ 副本可读 | `./task/` 软链（**只读**） | — | 读写 + 自动归集（或 cwd 直落 artifacts/） |
| 工具调用 | — | — | ✓ list / propose_decision / propose_open | — |

P5.1 三栏全空意味着茶客对 task **真的零感知** —— 这是 MVP 故意选的窄路径，把"任务"先打磨成纯 UI / 持久化概念，再分批让茶客接入。

---

## 7. 阶段化路线

### P5.1（MVP，目标：能开任务 / 挂产物 / 记决策；**一房间最多一任务**）

P5.1 严守"一条窄路径"：开任务 → 消息打 tag → 拷贝产物 → 记决策。切换 active、关闭任务、清回房间级、状态流转、事件历史、任务级摘要、多任务全部延后到 P5.2 —— MVP 实现者不用面对"切 active 时未完成 turn 怎么办"/"已关闭任务 thread 如何渲染"/"清空后用户怎么回去" 三个状态面。

**关键限制：一房间最多 1 个任务**。已有 task 时新建按钮禁用，tooltip 显示"P5.2 起支持多任务"。这条比"允许多开但隐身"更窄 —— 后者会制造用户找不回的任务（P5.1 没有任务列表 UI 列出旧 active），不符合 §9"不做过度设计"。

落盘只新增：
- `tasks/state.json`、`tasks/<id>/task.json`、`tasks/<id>/decisions.jsonl`、`tasks/<id>/artifacts/`
- `transcript.jsonl` 多 `task_id` 字段（可缺，向后兼容；server 端按 `state.json` 自动打 tag，inbound 不带，见 §4.4）
- （不写 `events.jsonl` / `tasks/<id>/summary.jsonl`，留待 P5.2）

事件 / inbound：见 §4.2 / §4.3 的 "P5.1" 段。

UI：见 §5.1 的窄面板（无下拉、无关闭、无切回，且**房间已有任意任务时**禁用 "+ 新任务" —— 看 `task_info.tasks.length > 0`，不看 active）。

实现项：

- `chahua/task.py` 数据结构 + tasks/ 落盘 + 加载宽容（未知字段 warn 忽略；见 §8.1）
- `tasks/state.json` 持久化 `active_task_id`，`task_open` 后立刻重发 `task_info`
- **加载时双向修复 state.json 与 task.json**（与 §10 同步）：① state.json 指向不存在 task → 清回 None；② state.json 缺 / 为空但有且仅有一个有效 task.json → 自动把它设为 active 并回写 state.json
- `open_task` inbound 在**房间已有任意任务**时拒绝 + emit NOTICE（"当前已有任务，请等 P5.2"）。判定按 `tasks/*/task.json` 存在性扫，**不依赖 state.json.active_task_id** —— 若 state.json 丢失 / 被清空，旧 task.json 仍是"已有任务"的真凭据，避免悄悄允许开第二个
- 前端任务面板按 `task_info.tasks.length > 0` 决定 "+ 新任务" 按钮 disabled（同样不看 active_task_id）；面板内容（当前任务卡 + artifacts + decisions）从 `tasks[0]` 取
- "新建任务"按钮 + `/task <title>` 斜杠命令（已有任务时斜杠命令也走拒绝路径）
- "把 share/ 文件**拷贝**到当前任务" 手动绑（copy 不 move，见 §4.3）
- "右键消息 → 标为决策"

### P5.2（多任务管理 —— 把 §4 / §5 / §10 里所有标 P5.2 的口都打开）

- 解除"一房间最多一任务"限制：`open_task` 不再拒绝，旧 active 留入历史任务列表
- 历史任务列表 / 切 active / 关闭任务 / 清回房间级
- 新事件 `task_close`；`task_update` 扩展接受 owner / status patch；新 inbound `set_active_task` / `close_task`
- `events.jsonl`（任务状态流转历史）
- 任务级 `summary.jsonl` + summarizer 复用现有 `Summarizer`
- 切 active 前要 cancel 当前 turn（沿用 P3.3 cancel 状态机）
- composer chip 升级成下拉
- **通道 2**（§6.2）首次落地：guest workspace 下 `./task/` 软链 / junction（**只读**），active 切换时重链，茶客能 read 当前任务产物。茶客 write 自动归任务延后到 P5.4
- **重构（顺手）：`chahua/server.py` 按 inbound feature 拆分为 mixin**。当前 2100+ 行 / 单 `ChahuaServer` 类承担 1500+ 行 / 30+ 个 `_inbound_*` handler；P5.2 新增 `set_active_task` / `close_task` / `task_close` emit 是合理触发点。建议切成：`server.py`（ws 生命周期 + 帧路由 + room snapshot）/ `server_inbound_admin.py`（guest/room/persona/permission）/ `server_inbound_task.py`（task 全套 inbound + `_emit_task_info` / `_emit_task_envelope`）/ `server_inbound_io.py`（upload / export / persona import）/ `server_inbound_settings.py`（USER.md / 头像 / room.toml）。约半天工作；P5.1 期间故意不动，避免与 P5.2 设计同时改触发耦合

### P5.3（茶客侧感知）

- **通道 1**（§6.1）：onboarding 注入 task 块（title / goal / status / 近期决策 / artifact 清单）；scoring 对 owner 加权
- **通道 3**（§6.3）：注册 `task_list_artifacts` / `task_propose_decision` / `task_propose_open` 三个 task-aware 工具
- 茶客 propose 的 UI 渲染："采纳"按钮把 propose 转译回 inbound `add_decision` / `open_task` —— **写权限永远在用户**

### P5.4（可选 / 远期）

- **通道 2 升级到读写 + 自动归集**：二选一 —— ① `./task/` 改 read-write，监听文件变化 / tool wrapper 拦截 write，合成 `task_artifact_added(created_by=<guest_name>)`；② 茶客 cwd 直接切到 `tasks/<id>/artifacts/`，废软链。两条路都要重设 `[[guest]].isolation` 语义，单独评估
- 任务级 `[task.llm]` 段覆盖房间默认
- 跨任务搜索 / 跨房间任务卡片

---

## 8. 关键不变量（沿 [CLAUDE.md](../CLAUDE.md) 口径）

1. **入站严格、落盘宽容**：
   - 入站 inbound payload（`open_task` / `update_task` / `add_decision` 等）字段白名单严格，未知键直接报错 + emit NOTICE 给前端 —— 等价 `room.toml` 抓 typo 的口径，**保护数据不被前端瞎写**。
   - `task.json` / `decisions.jsonl` / `events.jsonl` 加载时**未知字段 warn 后忽略**，必需字段缺失才跳坏该条 —— 等价 `transcript.jsonl` 跳坏行口径，**保护跨版本向前兼容**（新版本 schema 加字段不让老版本启动炸）。两条规则有意不对称。
2. **`transcript.jsonl` 加字段不破坏向后兼容**：缺 task_id = 房间级；加载跳坏行规则不变
3. **artifact / decision / task 的写权限只在用户**：茶客通过协议标签提议，UI 渲染成按钮等用户点确认；不允许茶客静默开任务 / 记决策（防 AI 自我接龙生成 50 个任务）
4. **API key / 秘密永不进 task.json 也不进 envelope**（沿用现有口径）
5. **`task_id` 只活在 `envelope.data`**，envelope 顶层字段不动，schema_version 不 bump
6. **单房间同时 0 或 1 个 active_task**：心智成本必须低于"打开多个聊天窗口"
7. **task 目录删除 = 硬删除**：和 room 删一致，不留 trash

---

## 9. 明确不做的事

- 不做任务依赖图 / 甘特 / 看板 —— Linear 的活
- 不做任务级权限隔离（哪位茶客只能看哪个任务）—— 复杂度爆炸
- 不做跨房间任务 —— 房间永远是任务的容器
- 不自动从聊天里 NLP 挖掘任务 —— 误报污染数据；用户必须显式
- 不引入消息线程分裂（Slack-style sub-thread）—— 破坏茶客打分的最近上下文连续性

---

## 10. 主要风险 & 兜底

P5.1 范围内：

| 风险 | 兜底 |
|---|---|
| 历史 transcript 没 task_id，迁移成本 | 不迁移：旧消息天然归"房间级闲聊"；新建任务后才开始挂 task_id；零侵入 |
| `tasks/state.json` 与 `<id>/task.json` 不一致 | 加载时双向兜底：① state.json 指向的 task_id 不存在 → 清回 None；② state.json 缺 / 为空且**有且仅有一个**有效 `task.json` → 自动把它设为 active 并回写 state.json（避免 UI 显示任务但新消息被打成房间级闲聊）。"存在多于一个" 在 P5.1 不会发生（§7.1 拒绝条款），但 P5.2 起会 —— 那时 ② 条要细化为"多于一个 → 不自动选，需 UI 二次确认" |
| 用户开了一个不想要的任务，但 P5.1 无清回入口 | 接受为 MVP 局限；user-facing copy 在"+ 新任务"按钮 tooltip 上明示"P5.1 暂不支持取消任务模式，等下版本"；power-user 兜底为手动删除 `rooms/<id>/tasks/` 目录重置（关停 sidecar → 删目录 → 重启） |

P5.2 引入（**这里 P5.1 不处理**）：

| 风险 | 兜底（P5.2 评估） |
|---|---|
| active_task 切换中途有 turn 在跑，产生"夹生"归属 | 切 active 前强制 cancel 当前 turn（已有 inbound `cancel` 帧）；前端切之前给确认 toast |
| artifact 上传时 task 被关闭，文件无主 | 上传瞬间快照 task_id；事后任务状态变化不动文件归属（attach 是 copy，原文件仍在 share/） |

P5.3 引入：

| 风险 | 兜底（P5.3 评估） |
|---|---|
| onboarding 注入 task 块后 prompt 膨胀，scoring 模型吃不下 | `onboarding_threshold` 在 task 模式下下调（task 块算 K 条预算）；scoring 仍只读最近 N 条原文不读 task 块 |

---

## 11. P5.1 commit checklist

把 §7.1 拆成 15 个独立可 review / 可回滚的 commit，按依赖顺序排。每个 commit 要求：① 单独跑 `uv run pytest` 不挂；② 单独装到 dev Electron 不卡 ws 握手（即使新 UI 缺失，旧路径不能坏）。

**约定**：依赖列里写 commit 编号或 "无"；commit message 前缀按 `chahua: P5.1.<n> ...` 沿用 P4 习惯。

| # | 范围 | 依赖 | 验收线索 | commit |
|---|---|---|---|---|
| **P5.1.1** | 新建 `chahua/task.py`：`Task` dataclass + `TaskStatus` enum + `from_jsonl` / `to_jsonl_dict`；加载未知字段 warn 忽略、必需字段缺失返 None。纯模块，**不接 wiring** | 无 | 单测：合法 jsonl round-trip / 未知字段加载成功 / 必需字段缺失返 None | `29033c8` |
| **P5.1.2** | `chahua/room.py` `Message` 加可选 `task_id` 字段（`Optional[str]`，缺省 None）；`Room.append` 多收 `task_id` 参数；`to_jsonl_dict` / `from_jsonl` 兼容旧行（缺字段 = None） | P5.1.1 | 单测：旧 transcript.jsonl（无 task_id）加载不变 / 新 append 带 task_id 落盘 round-trip | `29033c8` |
| **P5.1.3** | `chahua/_persist.py` 加 `write_json_atomic`（tmp+rename），给 task.json / state.json 用；保留 `append_jsonl` / `read_jsonl_skip_bad` 不动 | 无 | 单测：写到一半 crash 模拟（os.replace 前 raise）→ 原文件完整 | `29033c8` |
| **P5.1.4** | `chahua/tasks_store.py`（新文件）：`open_task` / `update_task` / `add_decision` / `attach_artifact` / `list_tasks` / `get_active_task` / `set_active`；加载时双向修复 state.json↔task.json（§7.1 实现项第 2 条）；`open_task` 在已有任意有效 task.json 时 raise `TaskExistsError` | P5.1.1, P5.1.3 | 单测：① 双向修复（state 缺 + 单 task.json 自动设 active 并回写）；② `open_task` 第二次被拒；③ attach 是 copy 不是 move（share/ 原文件 stat 不变） | `29033c8` |
| **P5.1.5** | `chahua/events.py` 加 5 个新 `ChahuaEventType`：`TASK_INFO` / `TASK_OPEN` / `TASK_UPDATE` / `TASK_DECISION_ADDED` / `TASK_ARTIFACT_ADDED`；不动 schema_version。`new_task_id()` / `new_decision_id()` helper | 无 | grep：value 字符串与 §4.2 表对齐 | `29033c8` |
| **P5.1.6** | `chahua/server.py` 装载 `TasksStore` per room（与 `Room` 同一 `RoomSession`）；ws 连上 / 切房时 `room_info` 后追发 `task_info`（即使为空 `{tasks: [], active_task_id: null}` 也下发，让前端知道协议生效） | P5.1.4, P5.1.5 | E2E：dev Electron 连上看 ws 帧含空 task_info | `29033c8` |
| **P5.1.7** | `server.py` 5 个新 inbound handler：`open_task` / `update_task` / `attach_artifact` / `add_decision`（**不含** `set_active_task` / `close_task`）；payload 严格白名单 + 不合法走 `NOTICE` level=error；成功后 emit hint 事件 + 重发 `task_info`（§4.2 事件分工） | P5.1.6 | 单测：① 未知字段 inbound → NOTICE；② `open_task` 在 active 已有时 → NOTICE；③ 合法 inbound → state.json + task.json 都落盘 | `29033c8` |
| **P5.1.8** | `chahua/orchestrator.py` `submit_user_message` 入口处从 `TasksStore.active_task_id` 取 tag，传到 `Room.append`；`turn_start` 时把 active 存到 turn 局部状态，本轮 `message_*` envelope 的 `data` 多带 `task_id`（沿用 §4.1） | P5.1.7 | 单测：active 非空时新消息 `message_id` 与 transcript 落盘 record 都带相同 task_id | `29033c8` |
| **P5.1.9** | 前端 `app/renderer/events.js` 加 5 个新 `EventType` 字符串常量 + `Inbound` 名加 4 个；`renderer.js` 收到 `task_info` 时存到 `taskState` 模块状态；初装 ws 时同 `room_info` 处理 | P5.1.6 | dev：F12 console 打 `__chahua_task_state` 能看到当前任务 | _PR 2_ |
| **P5.1.10** | 新建 `app/renderer/task_panel.js`：右侧任务面板 DOM（当前任务卡 + artifacts 列表 + decisions 列表）；折叠按钮记忆到 user prefs（与 P3 sidebar 折叠同口径）；`index.html` 加面板容器 + CSS | P5.1.9 | dev：手工触发 task_open（命令行 inbound）后面板出现；折叠状态重启保留 | _PR 2_ |
| **P5.1.11** | composer 任务 chip：active 非空时显示 "📋 <title>" 静态 pill（无下拉无 ✕）；`task_info.tasks.length > 0` 时 "+ 新任务" 按钮 disabled + tooltip "P5.2 起支持多任务"；点击按钮开 modal（title / goal / owner 选一可选） | P5.1.10 | dev：开 1 个任务 → chip 出现、按钮变灰；点按钮看 tooltip | _PR 2_ |
| **P5.1.12** | 决策入口：消息气泡右键 → "📌 标为决策" → 弹小框（summary textarea + 可选连续往上勾选支持消息）→ inbound `add_decision`；面板 decisions 列表 hint 高亮新加项 | P5.1.10 | dev：标一条决策 → 面板出现 / decisions.jsonl 新行 | _PR 3_ |
| **P5.1.13** | artifact 入口：share/ 文件 pill 上加 "拷贝到当前任务" 动作（active 非空才显示）→ inbound `attach_artifact`；面板 artifacts 列表 hint 高亮新加项；任务面板 inline 编辑 title / goal（`contenteditable` + blur 触发 update_task） | P5.1.11 | dev：上传文件 → 拷贝到任务 → share/ 原文件仍在 + task artifacts/ 副本出现 | _PR 3_ |
| **P5.1.14** | `/task <title>` 斜杠命令：composer textarea 提交前拦截以 `/task ` 起首的行；命令拼出 `open_task` inbound（goal 空 / owner null）；已有任务时走拒绝路径（同 §7.1） | P5.1.11 | dev：输入 `/task 写 README` → 任务创建 | _PR 3_ |
| **P5.1.15** | doc 同步：更新 `CLAUDE.md` "关键不变量" 段（加 task.json 白名单 / 入站严格落盘宽容口径）；`docs/CHANGELOG`（如果有）加 P5.1；本文件 §11 表里补 commit hash 列 | 全部 P5.1.* | 跑 `uv run pytest` 全绿；dev Electron 走一遍开任务 / 标决策 / 挂产物 / 重启恢复 active 流程 | _PR 3_ |

**分批 PR 建议**：13 个 commit 分 3 个 PR 推：
- PR 1（后端骨架）：P5.1.1 ~ P5.1.8 —— 全部 server / 持久化 / orchestrator 接入，UI 没动还是房间级
- PR 2（前端面板）：P5.1.9 ~ P5.1.11 —— 任务面板 + composer chip 出现，能开任务但还没 artifact / decision 入口
- PR 3（功能闭环）：P5.1.12 ~ P5.1.15 —— 决策 / artifact / 斜杠命令 / doc 同步

PR 1 合入后即使 PR 2 / PR 3 还没跟上，前端**完全不感知**也不报错（hint 事件 / `task_info` 都被忽略），是个真正的 incremental ship 路径。

> commit 实际策略：PR 1 (P5.1.1~8) 在 `29033c8` 一次合并落地；PR 2 / PR 3 计划各自一次提交，hash 待提交后回填上表。
